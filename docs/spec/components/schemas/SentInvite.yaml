type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - id
      - type
      - attributes
    properties:
      id:
        type: string
        format: uuid
        description: "invite id"
      type:
        type: string
        enum: [ distributor_invite ]
      attributes:
        type: object
        required:
          - distributor_id
          - role
        properties:
          distributor_id:
            type: string
            format: uuid
            description: "ID of the distributor the invite is for"
          role:
            type: string
            description: "Role assigned to the invited user"